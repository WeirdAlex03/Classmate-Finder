
/**
 * Parses form submission and sends it to the database server
 * 
 * @param {ScriptApp.AuthMode} authMode
 * @param {FormResponse} response
 * @param {Form} source
 * @param {int} triggerUid
 * 
 */
function onSubmit(authMode, response, source, triggerUid) {	//jshint ignore:line

}

